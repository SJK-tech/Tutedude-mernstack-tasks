# Tute Task 4

A simple HTML and CSS webpage that displays an image with a gradient-styled button using viewport units (`vw`, `vh`) and the CSS box model. 

1. Download or clone the repository.
2. Open the `tute task4` folder.
3. Open `index.html` in any modern web browser.
